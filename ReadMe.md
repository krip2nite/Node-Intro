dd if=/dev/zero of=large_file bs=1M count=500
creates large file of 0's 500mb sized

time npm run dev
in git bash, gives an option to see what time is needed

time npm run dev > file.txt
redirection into a file